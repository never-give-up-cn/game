# 大量渲染、tick逻辑依赖 inventory / map，但没有前向引用处理
# GameWindow主文件又导入 BuildingBase、MapGrid
解决方案：
所有只做类型注解的地方使用字符串前向引用，或者 from __future__ import annotations 放在每个文件头部；
运行时实例传递，不要模块顶层互相 import 实体类；
不要在基类文件里导入上层 UI、窗口模块。
2. Inserter.tick() 机械臂抓取逻辑完全错误（核心物流失效）
python
运行
def _do_grab(self, inventory):
    # ❌ 重大错误：机械臂不应该直接从玩家inventory抓取！
    # 设计目标：机械臂抓取【地图上建筑/传送带】内物品，现在写死读玩家背包
    for iid in list(inventory.slots):
当前实现：机械臂凭空从玩家背包拿东西，完全不符合物流逻辑；
缺失：拾取位置 pickup_pos 的建筑查询，需要从地图上目标建筑的缓冲区读取物品，而不是玩家背包；
_do_drop 同理，直接丢进玩家背包，错误。
3. 传送带 ConveyorBelt 只有物品存储，没有相邻建筑收发逻辑
ConveyorBelt.tick() 只负责内部物品移动，** 没有实现：
机械臂向传送带放入物品
传送带把物品送出到下一格传送带 / 建筑 **
传送带物品永远只会在自身格子内部循环，无法整条链路传输，物流链路断裂。
4. BuildingBase.produce() 设计缺陷：直接操作全局玩家 inventory
python
运行
def produce(self, inventory) -> bool:
    for item_id, amount in self.inputs.items():
        inventory.remove_item(item_id, amount)
问题：
建筑理论上应该拥有独立内部缓冲区，而不是直接消耗玩家背包。
当前交互逻辑混淆两种模式：
1）原版思路：建筑自带输入缓存，机械臂运货到建筑缓存；
2）你现在代码：建筑直接啃玩家背包，只适合手动投放模式，自动化物流完全无法适配。
你写了 self.input_buffer / output_buffer 但是根本没有被生产逻辑使用，属于写了变量闲置，代码冗余。
5. 拆除逻辑边界漏洞
python
运行
def _tick_demolish(self):
    # 拆除成功后只移除建筑，没有掉落材料！
    self.game_map.remove_building(bld)
拆除建筑不会返还建造材料，工厂类游戏核心机制缺失；
同时缺少判断：正在生产的建筑拆除是否增加耗时、是否允许中途拆除。
6. 资源矿脉开采逻辑缺失
ResourceManager 定义了 mine_at()，但是游戏内没有任何代码调用开采；
玩家走到矿脉上无法采集，采集交互逻辑完全空白。
7. 科技树研究队列逻辑不完整
ResearchQueue 只有队列存储，没有每帧消耗科研瓶推进进度的 tick
python
运行
# GameWindow._update_movement() 完全没有调用科研队列tick
# rq.progress 永远不会自动上涨
打开科技树只能添加任务，但时间不会流逝，研究永远卡住。
二、逻辑设计缺陷（架构层面，后续扩展很难受）
1. 全局唯一玩家 Inventory 强耦合
所有建筑 tick、机械臂、交互全部传递 player.inventory。
未来想要支持多玩家、无人自动化（建筑独立仓储） 完全无法扩展。
理想模型：
每个生产建筑拥有独立内部库存
玩家背包只是移动容器
物流机械臂只在两个容器之间搬运（容器抽象接口 InventoryContainer）
2. 缺少统一「物品容器抽象」
玩家背包、建筑输入缓存、传送带、箱子，都是物品容器，但没有抽象基类。
导致机械臂、传送带代码需要写大量 if 区分目标类型，代码重复。
3. 建筑大类没有继承体系区分
现在只有单一 BuildingBase，靠模板字段区分：
传送带、机械臂、生产工厂、城墙全部是同一个父类。
建议拆分：
BuildingBase（基类）
ProductionBuilding 生产建筑（工厂、熔炉，有输入输出配方）
LogisticBuilding 物流建筑（传送带、分流器、机械臂）
StorageBuilding 仓储（仓库）
DefenseBuilding 防御建筑（城墙、炮塔）
现状：靠 hasattr(bld, "lanes")、"机械臂" in name 这种字符串判断类型，非常脆弱，改名字代码直接崩。
4. ITEM_TO_BUILDING 映射冲突
python
运行
ITEM_TO_BUILDING = {
    "iron": "工厂",
    # 下面覆盖同一个key
    "iron": "电力机械臂",
}
字典重复 key，后面直接覆盖前面，铁锭只能放置机械臂，无法放置工厂，放置功能 BUG。
三、渲染与 UI 相关问题
1. 大量渲染代码写在 GameWindow 巨型类中
GameWindow 几千行代码，所有绘制、事件、逻辑堆在一起，违反单一职责。
建议拆分：
InputHandler：事件处理
Renderer：所有绘制函数
GameWorld：纯游戏逻辑（不含 pygame）
2. 弹窗渲染耦合
BuildingBase.render_popup 嵌入大量 pygame 绘制代码；
业务模型层（building.base）不应该依赖 pygame 图形库！
严重分层违规：数据模型不应该持有视图渲染逻辑
正确做法：模型只提供数据，UI 层读取建筑数据进行绘制。
3. 坐标两套体系容易混淆
世界网格坐标（gx, gy）
屏幕视口坐标
多处转换没有统一封装工具函数，极易出现 ±1 偏移 BUG。
4. 动画帧 anim_frame 全局单例
所有建筑进度条、呼吸光效共用同一帧计数器，未来想要独立动画速度很难调整。
四、代码冗余 & 无效代码
BuildingBase.input_buffer / output_buffer 定义但全程未参与生产逻辑，摆设变量；
BuildingBase.quality 基础属性定义，但生产计算几乎没有用到品质修正；
配方系统 MACHINE_RECIPES 存在，但是生产建筑没有绑定配方；
当前建筑 inputs/outputs 写死在 BUILDING_TEMPLATES，无法动态切换配方（不能更换产物，不能造不同物品）；
理想：生产建筑持有当前选中配方 ID，从配方库读取输入输出。
Recipe 合成类存在，但是建筑生产没有复用 Recipe，重复写一套输入输出；
地面物品 _ground_items 只实现放置、拾取，没有渲染数量上限、超时消失机制。
五、性能隐患
每帧遍历全部建筑 for b in self.game_map.buildings[:]:
后期大量建筑后帧率暴跌；可增加激活范围，只 tick 视口附近建筑；
_building_at(gx,gy) 每次遍历全部建筑列表线性查找；
优化：建立空间哈希字典 {(gx,gy): building}，空间分区索引；
区块生成 Chunk 每次访问动态生成，但是资源生成随机种子绑定简单坐标，远距离区块资源模式重复。
六、小 BUG 与细节问题
地下传送带 UndergroundBelt 只有配对标记 self.paired，没有自动寻找出入口的匹配逻辑；
分流器 Splitter 的 input_buf/output_buf 只有内部缓冲，不能和传送带互相传递物品；
热能机械臂燃料逻辑：只实现添加燃料，没有 UI 界面添加燃料入口；
筛选机械臂 filter_items 存在属性，但是没有 UI 弹窗修改过滤名单；
建造材料 construction_materials 在模板定义，但放置建筑时完全没有扣材料！
现在放置建筑只消耗手持建筑道具，没有读取模板内建造材料校验；
星球切换逻辑写死在 get_planet_at() 坐标分区，没有火箭跃迁交互。

1. tick 遍历顺序致命 BUG
python
运行
# 现在代码
for item in lane:
    item["p"] += spd
Python for item in lane 遍历列表同时修改转移、pop，如果一帧多个物品 p≥1，处理顺序混乱。
规范做法：先推进所有 progress → 从后往前遍历（从车头到车尾）处理转移，防止一帧内物品互相穿越。
2. 物品转移逻辑缺陷：只允许车头单个物品跨格
原版异星工厂：高速皮带一帧理论可以送出多个物品。
当前逻辑每次最多转移一个；虽然游戏观感影响不大，但机制不严谨。
3. _target_lane 转弯映射逻辑不完善
现在只区分直走 / 左转 / 右转，但缺少校验：
只有连续同向转弯链路才允许车道映射；
如果皮带是反向对接、十字交叉、斜向，不应该互通。
目前只要 front_pos 找到传送带就尝试传输，侧向相邻的皮带会错误传货。
4. 容量判断时机错误
add_item 限制插入车尾最多 4 个，但是：
物品转移进来 tgt.insert(0, head)，没有提前判断目标车道长度！
极端情况目标车道瞬间超过 4 个，突破 capacity。
5. 断头逻辑没问题，但缺少「相邻机械臂拾取判定」
不影响运行，属于扩展点。
6. 渲染配套已经写好了
GameWindow 里 _draw_building_cell 已经读取 item.get("p",0.0)，渲染层适配完成，这部分不用改。
二、Splitter 现存严重 BUG
python
运行
if len(self.output_buf[out]) < self.capacity:
    self.output_buf[out].append(self.input_buf[lane].pop(0))
❌ 当前代码：先 pop 物品，再判断输出能不能放下！
放不下的情况下，物品已经从输入缓冲区移除 → 直接凭空消失！
必须调整顺序：先预判输出容量，满足条件再 pop。
额外优化点：
toggle_l / toggle_r 独立车道计数器思路正确；
分流逻辑「输入左只参与左车道分流、输入右只参与右车道」符合异星工厂；
缺少：分流器输出同样要能向下游传送带传输（现在只有输入接收，没有输出推送 tick 逻辑！）
👉 现在分流器只会堆积物品，不会把物品送给下游传送带！重大缺失
三、还有一个全局时序问题
python
运行
def _tick_buildings(self):
    inv = self.player.inventory
    for b in self.game_map.buildings[:]:
        b.tick(inv)
遍历顺序随机！物流游戏强制执行顺序：
分流器 → 传送带 → 机械臂 → 工厂
乱序会出现：同一帧传送带刚送出物资，分流器还没处理，或者物资一进一出瞬移。

但是还残留 6 处逻辑漏洞 + 边界缺陷，会出现堵料异常、物资不传输、转弯车道映射错误、分流器对接校验缺失、地下传送带未实现、机械臂对接接口不完善。下面逐条列出，附带原因和修复方案。
1. ConveyorBelt._find_next () 对接校验不完整【高危】
python
运行
# 当前代码
dx, dy = DIR_VEC[b.direction]
entry_x = b.x + dx
entry_y = b.y + dy
if entry_x == self.x and entry_y == self.y:
    return b
逻辑含义：下游传送带的入口正对当前传送带出口才允许对接。
✅ 思路正确，但是方向判断写反：
当前皮带 self 方向：向外输出（front_pos）
下游皮带 b 需要朝向 self，也就是：b.front_pos == (self.x, self.y)
你现在计算 entry_x = b.x + dx，dx/dy 是 b 前进方向，等价于 b.front_pos，表达式数学上没问题，但可读性极差，极易后续改代码改错。
更致命：没有判断格子坐标相等之外，没有防止自环、远距离匹配（虽然地图不会叠加建筑，但规范上建议加固）。
优化写法：
python
运行
fx_self, fy_self = self.front_pos
if (b.x, b.y) == (fx_self, fy_self):
    # 下游出口必须指向自己
    fx_b, fy_b = b.front_pos
    if (fx_b, fy_b) == (self.x, self.y):
        return b
2. ConveyorBelt 转弯车道映射 _target_lane 规则存在缺陷【中危】
python
运行
if turn_r: return "left" if my_lane == "left" else "right"
if turn_l: return "right" if my_lane == "left" else "right"
这条映射规则只是简单原样返回车道名，没有实现标准异星工厂皮带转弯内外轨切换：
右转：左车道→左车道（内轨）、右车道→右车道
左转：左车道→右车道、右车道→左车道（车道互换）
当前左转时直接返回 "right"，my_lane=="right" 依然返回 right，车道不会交换。
结果：连续左转的传送带上物品会两车道重叠、互相卡位，视觉与物流拥堵异常。
修正函数：
python
运行
def _target_lane(self, my_lane, my_dir, next_dir):
    if my_dir == next_dir:
        return my_lane
    turn_right = (my_dir + 1) % 4 == next_dir
    turn_left = (my_dir - 1) % 4 == next_dir
    if turn_right:
        # 右转：车道保持不变
        return my_lane
    if turn_left:
        # 左转：车道互换
        return "right" if my_lane == "left" else "left"
    return my_lane
3. Splitter 车道映射硬编码 L→L，缺少转弯适配【中危】
python
运行
tl = lane
if len(next_belt.lanes.get(tl, [])) < next_belt.capacity:
分流器输出到传送带时，直接使用原车道名称。
如果分流器正后方的传送带是转弯皮带，必须调用 next_belt._target_lane() 做车道转换，和皮带之间传输规则统一。
否则：分流器送出的物品落在错误车道，卡在转弯处无法流动。
tick 输出推送段修改：
python
运行
# 原
# tl = lane
# 修改为
tl = next_belt._target_lane(lane, self.direction, next_belt.direction)
4. Splitter._find_output_belt 封装简陋，无法复用【低危】
python
运行
def _find_output_belt(self, lane):
    out_dir = self.direction
    dx, dy = DIR_VEC[out_dir]
    fx, fy = self.x + dx, self.y + dy
    if not self.game_map: return None
    for b in self.game_map.buildings:
        if isinstance(b, ConveyorBelt) and b.x == fx and b.y == fy:
            return b, lane
    return None
问题：返回元组 (belt, lane)，但车道转换逻辑放在外层，耦合混乱。
建议只返回下游传送带实例，车道映射统一在外层调用 _target_lane，逻辑集中、便于调试。
5. UndergroundBelt 仅有外壳，完全没有隧道传输逻辑【重要扩展缺失】
当前地下传送带 UndergroundBelt 只是 UI 弹窗，没有：
出入口配对扫描（沿 direction 直线搜索指定距离寻找另一端）
物品缓冲区
tick 隧道传送逻辑
现状：摆出来只是贴图，不能运输物资，后续你搭建长距离管线会断层。
如果你短期内不用地下皮带可以暂时搁置，但要记在 TODO 清单。
6. 机械臂和传送带交互接口完整度不足（F 拾取是临时方案）【架构隐患】
当前拾取逻辑：_pickup_to_cursor 直接调用 lane.pop(-1)
真实生产循环应当：
Inserter（机械臂）tick 主动查询相邻皮带
调用统一接口 belt.take_item(lane) 从车头取出
提供 belt.receive_item(item, lane) 放入车尾
现在硬编码 pop (-1)，未来如果新增 “禁止玩家直接拾取、只能机械臂操作”、优先级过滤等规则时，代码分散难以维护。
建议给 ConveyorBelt 增加标准化 API：
python
运行
def take_front_item(self, lane: str):
    """从车头取出物品，无物品返回None"""
    ln = self.lanes.get(lane)
    if not ln:
        return None
    return ln.pop(-1)

def put_back_item(self, item_id: str, lane: str):
    return self.add_item(item_id, lane)
7. 一处边界极端情况：皮带同时多个物品到达出口，下游瞬间堵死（性能 / 拥堵模拟）
当前逆序遍历支持一帧多个物品送出，这点没问题。
但存在场景：高速皮带一帧送出多件，下游容量只剩 1 个。
表现：靠前的物品成功转移，后面物品停留在 p=1。逻辑上符合物理规则，不属于 bug，属于设计取舍，不用修改。
额外：GameWindow 侧一处微小时序隐患
python
运行
def _tick_buildings(self):
    inv = self.player.inventory
    from conveyor import Splitter, ConveyorBelt
    from inserter import Inserter
    for b in self.game_map.buildings:
        if isinstance(b, Splitter): b.tick(inv)
    for b in self.game_map.buildings:
        if isinstance(b, ConveyorBelt): b.tick(inv)
    for b in self.game_map.buildings:
        if isinstance(b, Inserter): b.tick(inv)
    for b in self.game_map.buildings:
        if not isinstance(b, (Splitter, ConveyorBelt, Inserter)):
            b.tick(inv)
循环遍历四次 buildings 列表。建筑数量巨大时轻微浪费性能。
优化方案：一次遍历，分类执行：
python
运行
def _tick_buildings(self):
    inv = self.player.inventory
    from conveyor import Splitter, ConveyorBelt
    from inserter import Inserter
    splitters = []
    belts = []
    inserters = []
    others = []
    for b in self.game_map.buildings:
        if isinstance(b, Splitter):
            splitters.append(b)
        elif isinstance(b, ConveyorBelt):
            belts.append(b)
        elif isinstance(b, Inserter):
            inserters.append(b)
        else:
            others.append(b)
    for b in splitters: b.tick(inv)
    for b in belts: b.tick(inv)
    for b in inserters: b.tick(inv)
    for b in others: b.tick(inv)